var express = require('express');
var router = express.Router();

/* GET home page. */
router.get('/', function(req, res) {
  res.render('index', { title: 'Express' });
});

/* GET game page. */
router.get('/hello', function(req, res){
  res.render('hello', {title: 'Game' });
});

module.exports = router;
